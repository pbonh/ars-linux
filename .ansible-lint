profile: production
exclude_paths:
  - .github/
  - tests/
  - docs/
skip_list:
  - schema[meta]                # meta/main.yml not required for personal roles
  - role-name                   # we use snake_case throughout
  - var-naming[no-role-prefix]  # project-wide ars_ prefix is intentional
warn_list:
  - experimental

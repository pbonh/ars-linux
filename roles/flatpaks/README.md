# flatpaks

> **TODO before first deploy:** replace `files/flathub.flatpakrepo` with the real file from `https://dl.flathub.org/repo/flathub.flatpakrepo` (the placeholder GPG key will fail flatpak's signature check).

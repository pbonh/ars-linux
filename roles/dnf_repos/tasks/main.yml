---
- name: Render DNF repo files into /etc/yum.repos.d
  ansible.builtin.template:
    src: "{{ item.value }}"
    dest: "{{ ars_dnf_repos_dir }}/{{ item.key }}"
    owner: root
    group: root
    mode: "0644"
  loop: "{{ ars_dnf_repos | dict2items }}"
  loop_control:
    label: "{{ item.key }}"

# containers_policy

> **TODO before first deploy:** replace `files/pki/zirconium.pub` with the actual upstream Zirconium cosign public key (mkosi.keys/cosign.pub from the upstream repo). The placeholder will fail `bootc container lint` and real cosign verification.
